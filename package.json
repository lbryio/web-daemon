{
  "author": {
    "email": "paul@lbry.io",
    "name": "Paul Anthony Webb"
  },
  "dependencies": {
    "app-root-path": "^2.1.0",
    "axios": "^0.18.0",
    "base64-img": "^1.0.4",
    "graceful-fs": "^4.1.11",
    "micro": "^9.3.3",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.5",
    "vne": "^1.2.0"
  },
  "description": "Microservice for LBRY",
  "devDependencies": {
    "husky": "^1.0.1",
    "micro-dev": "^3.0.0",
    "npm-run-all": "^4.1.3",
    "snazzy": "^8.0.0",
    "standardx": "^3.0.1",
    "updates": "^4.3.0"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run test:lint"
    }
  },
  "name": "web-daemon",
  "private": true,
  "scripts": {
    "deploy": "vagrant push",
    "inspect": "node --inspect node_modules/.bin/micro-dev --port 5200",
    "start": "npm i --no-audit && micro -l tcp://localhost:5200",
    "watch": "micro-dev -w routes -p ${PORT:-5200}",
    "test": "run-s test:*",
    "test:dependencies": "updates -u ./",
    "test:lint": "standardx --verbose | snazzy"
  },
  "version": "1.0.0"
}
